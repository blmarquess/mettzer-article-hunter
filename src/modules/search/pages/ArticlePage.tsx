export const ArticlePage = () => {
  // const articleID = useParams<{ id: string }>().id
  // const { data, isFetching } = useArticleQuery(articleID)
  return (
    <div>
      {' '}
      <h1>Pagina de Artigo único</h1>
    </div>
  )
}
